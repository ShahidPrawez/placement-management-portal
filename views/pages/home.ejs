<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Placement Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Base Styles */
        :root {
            --primary: #4361ee;
            --primary-dark: #3a56d4;
            --secondary: #7209b7;
            --success: #4cc9f0;
            --danger: #f72585;
            --warning: #f8961e;
            --info: #4895ef;
            --light: #f8f9fa;
            --dark: #212529;
            --gray: #6c757d;
            --light-gray: #e9ecef;
            --white: #ffffff;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --radius: 8px;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f5f7fb;
            color: var(--dark);
            line-height: 1.6;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Styles */
        header {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--white);
            padding: 1rem 0;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
        }

        .logo i {
            margin-right: 10px;
            font-size: 2rem;
        }

        .nav-links {
            display: flex;
            gap: 20px;
        }

        .nav-links a {
            color: var(--white);
            text-decoration: none;
            font-weight: 500;
            padding: 8px 16px;
            border-radius: var(--radius);
            transition: var(--transition);
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 2px;
            background-color: var(--white);
            transform: translateX(-50%);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        .nav-links a:hover::after {
            width: 80%;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(rgba(67, 97, 238, 0.9), rgba(114, 9, 183, 0.9)), url('https://images.unsplash.com/photo-1521737711867-e3b97375f902?ixlib=rb-4.0.3&auto=format&fit=crop&w=1200&q=80');
            background-size: cover;
            background-position: center;
            color: var(--white);
            padding: 5rem 0;
            text-align: center;
            margin-bottom: 3rem;
            border-radius: 0 0 var(--radius) var(--radius);
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .hero p {
            font-size: 1.2rem;
            max-width: 700px;
            margin: 0 auto 2rem;
        }

        /* Button Styles */
        .btn {
            display: inline-block;
            padding: 12px 28px;
            border: none;
            border-radius: var(--radius);
            font-weight: 600;
            text-decoration: none;
            text-align: center;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1rem;
            box-shadow: var(--shadow);
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            z-index: 0;
        }

        .btn:hover::before {
            transform: translateX(0);
        }

        .btn-primary {
            background-color: var(--primary);
            color: var(--white);
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(67, 97, 238, 0.4);
        }

        .btn-primary:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-secondary {
            background-color: var(--secondary);
            color: var(--white);
        }

        .btn-secondary:hover {
            background-color: #5a08a0;
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(114, 9, 183, 0.4);
        }

        .btn-secondary:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-success {
            background-color: var(--success);
            color: var(--dark);
        }

        .btn-success:hover {
            background-color: #3ab0d9;
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(76, 201, 240, 0.4);
        }

        .btn-success:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-danger {
            background-color: var(--danger);
            color: var(--white);
        }

        .btn-danger:hover {
            background-color: #e11574;
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(247, 37, 133, 0.4);
        }

        .btn-danger:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-warning {
            background-color: var(--warning);
            color: var(--dark);
        }

        .btn-warning:hover {
            background-color: #e0861b;
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(248, 150, 30, 0.4);
        }

        .btn-warning:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-outline {
            background-color: transparent;
            border: 2px solid var(--primary);
            color: var(--primary);
        }

        .btn-outline:hover {
            background-color: var(--primary);
            color: var(--white);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 16px rgba(67, 97, 238, 0.4);
        }

        .btn-outline:active {
            transform: translateY(-1px) scale(0.98);
        }

        .btn-lg {
            padding: 16px 32px;
            font-size: 1.1rem;
        }

        .btn-sm {
            padding: 8px 16px;
            font-size: 0.9rem;
        }

        /* Ripple effect */
        .ripple {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.6);
            transform: scale(0);
            animation: ripple-animation 0.6s ease-out;
            pointer-events: none;
        }

        @keyframes ripple-animation {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }

        /* Card Styles */
        .card {
            background-color: var(--white);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            overflow: hidden;
            transition: var(--transition);
            margin-bottom: 2rem;
            height: 100%;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

        .card-header {
            padding: 1.5rem;
            background-color: var(--primary);
            color: var(--white);
            font-weight: 600;
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-footer {
            padding: 1rem 1.5rem;
            background-color: var(--light-gray);
            border-top: 1px solid #dee2e6;
        }

        /* Features Section */
        .features {
            padding: 4rem 0;
            background-color: var(--white);
        }

        .feature-card {
            text-align: center;
            padding: 2rem;
            border-radius: var(--radius);
            transition: var(--transition);
        }

        .feature-card:hover {
            transform: translateY(-10px);
        }

        .feature-icon {
            font-size: 3rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
        }

        /* Stats Section */
        .stats {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--white);
            padding: 4rem 0;
            text-align: center;
        }

        .stat-item {
            padding: 1.5rem;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1.2rem;
        }

        /* Testimonials */
        .testimonials {
            padding: 4rem 0;
            background-color: var(--light);
        }

        .testimonial-card {
            padding: 2rem;
            background-color: var(--white);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
        }

        .testimonial-text {
            font-style: italic;
            margin-bottom: 1.5rem;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
        }

        .author-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background-color: var(--primary);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-weight: bold;
            margin-right: 1rem;
        }

        /* Section Styles */
        .section {
            margin-bottom: 3rem;
        }

        .section-title {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            color: var(--primary);
            position: relative;
            padding-bottom: 10px;
            text-align: center;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 4px;
            background-color: var(--primary);
            border-radius: 2px;
        }

        /* Grid System */
        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
        }

        .col {
            flex: 1;
            padding: 0 15px;
        }

        .col-2 {
            flex: 0 0 50%;
            padding: 0 15px;
        }

        .col-3 {
            flex: 0 0 33.333%;
            padding: 0 15px;
        }

        .col-4 {
            flex: 0 0 25%;
            padding: 0 15px;
        }

        /* Badge Styles */
        .badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-align: center;
        }

        .badge-primary {
            background-color: var(--primary);
            color: var(--white);
        }

        .badge-secondary {
            background-color: var(--secondary);
            color: var(--white);
        }

        .badge-success {
            background-color: var(--success);
            color: var(--dark);
        }

        .badge-danger {
            background-color: var(--danger);
            color: var(--white);
        }

        .badge-warning {
            background-color: var(--warning);
            color: var(--dark);
        }

        /* Footer Styles */
        footer {
            background-color: var(--dark);
            color: var(--white);
            padding: 3rem 0 1.5rem;
            margin-top: 3rem;
        }

        .footer-content {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .footer-section {
            flex: 1;
            min-width: 250px;
            margin-bottom: 2rem;
        }

        .footer-section h3 {
            margin-bottom: 1.5rem;
            font-size: 1.3rem;
            color: var(--success);
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section ul li {
            margin-bottom: 10px;
        }

        .footer-section a {
            color: var(--light-gray);
            text-decoration: none;
            transition: var(--transition);
        }

        .footer-section a:hover {
            color: var(--success);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Utility Classes */
        .text-center {
            text-align: center;
        }

        .text-right {
            text-align: right;
        }

        .mb-1 {
            margin-bottom: 1rem;
        }

        .mb-2 {
            margin-bottom: 2rem;
        }

        .mt-1 {
            margin-top: 1rem;
        }

        .mt-2 {
            margin-top: 2rem;
        }

        .p-1 {
            padding: 1rem;
        }

        .p-2 {
            padding: 2rem;
        }

        .d-flex {
            display: flex;
        }

        .justify-between {
            justify-content: space-between;
        }

        .align-center {
            align-items: center;
        }

        /* Responsive Design */
        /* Responsive Design */
        .mobile-nav-toggle {
            display: none;
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
                flex-direction: column;
                position: absolute;
                top: 70px;
                left: 0;
                right: 0;
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                padding: 20px;
                z-index: 99;
                animation: slideDown 0.3s ease-out;
            }

            .nav-links.active {
                display: flex;
            }

            @keyframes slideDown {
                from {
                    opacity: 0;
                    transform: translateY(-10px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

            .nav-links a {
                padding: 12px 16px;
                margin: 4px 0;
                border-radius: 6px;
                animation: fadeInLeft 0.3s ease-out forwards;
            }

            .nav-links a:nth-child(1) { animation-delay: 0.05s; }
            .nav-links a:nth-child(2) { animation-delay: 0.1s; }
            .nav-links a:nth-child(3) { animation-delay: 0.15s; }
            .nav-links a:nth-child(4) { animation-delay: 0.2s; }
            .nav-links a:nth-child(5) { animation-delay: 0.25s; }
            .nav-links a:nth-child(6) { animation-delay: 0.3s; }

            @keyframes fadeInLeft {
                from {
                    opacity: 0;
                    transform: translateX(-20px);
                }
                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            .mobile-nav-toggle {
                display: block;
            }

            .header-content {
                justify-content: space-between;
            }

            .hero h1 {
                font-size: 2.2rem;
            }

            .col-2, .col-3, .col-4 {
                flex: 0 0 100%;
                margin-bottom: 20px;
            }

            .footer-section {
                flex: 0 0 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <i class="fas fa-graduation-cap"></i>
                    PlacementMS
                </div>
                <button class="mobile-nav-toggle" aria-controls="primary-navigation" aria-expanded="false">
                    <i class="fas fa-bars"></i>
                </button>
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="#features">Features</a>
                    <a href="#about">About</a>
                    <a href="#testimonials">Testimonials</a>
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">Register</a>
                </div>
            </div>
        </div>
    </header>

    <div class="hero">
        <div class="container">
            <div class="hero-grid">
                <div class="hero-left">
                    <h1>Welcome to Placement Management System</h1>
                    <p>A comprehensive platform connecting students with top companies for career opportunities</p>
                    <div class="d-flex" style="gap:12px;justify-content:center;">
                        <a href="/auth/login" class="btn btn-cta btn-lg">Login</a>
                        <a href="/auth/register" class="btn btn-outline btn-lg">Register</a>
                    </div>
                        <!-- Partner logos removed per request -->
                </div>
                
            </div>
        </div>
    </div>

    <div class="container">
        <!-- Features Section -->
        <section id="features" class="features">
            <h2 class="section-title">Key Features</h2>
            <div class="row">
                <div class="col-3">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <h3>Student Profiles</h3>
                        <p>Create comprehensive profiles showcasing skills, education, and experience to attract top employers.</p>
                    </div>
                </div>
                <div class="col-3">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-building"></i>
                        </div>
                        <h3>Company Portal</h3>
                        <p>Companies can post job openings, schedule interviews, and manage candidate applications.</p>
                    </div>
                </div>
                <div class="col-3">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-chart-line"></i>
                        </div>
                        <h3>Analytics Dashboard</h3>
                        <p>Track placement statistics, application trends, and success rates with detailed analytics.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Stats Section -->
        <section class="stats">
            <div class="row">
                <div class="col-3">
                    <div class="stat-item">
                        <div class="stat-number">500+</div>
                        <div class="stat-label">Companies Registered</div>
                    </div>
                </div>
                <div class="col-3">
                    <div class="stat-item">
                        <div class="stat-number">10,000+</div>
                        <div class="stat-label">Students Placed</div>
                    </div>
                </div>
                <div class="col-3">
                    <div class="stat-item">
                        <div class="stat-number">95%</div>
                        <div class="stat-label">Placement Rate</div>
                    </div>
                </div>
                <div class="col-3">
                    <div class="stat-item">
                        <div class="stat-number">50+</div>
                        <div class="stat-label">Partner Universities</div>
                    </div>
                </div>
            </div>
        </section>

        <!-- About Section -->
        <section id="about" class="section">
            <h2 class="section-title">About Placement Management System</h2>
            <div class="row">
                <div class="col-2">
                    <div class="card">
                        <div class="card-body">
                            <h3>Our Mission</h3>
                            <p>To bridge the gap between academic institutions and industry by providing a seamless platform that connects talented students with leading companies, facilitating successful career placements.</p>
                        </div>
                    </div>
                </div>
                <div class="col-2">
                    <div class="card">
                        <div class="card-body">
                            <h3>System Roles</h3>
                            <ul>
                                <li><strong>Students:</strong> Create profiles, apply to jobs, track applications</li>
                                <li><strong>Companies:</strong> Post jobs, review applications, schedule interviews</li>
                                <li><strong>Administrators:</strong> Manage users, generate reports, system configuration</li>
                                <li><strong>Placement Officers:</strong> Coordinate between students and companies</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Testimonials Section -->
        <section id="testimonials" class="testimonials">
            <h2 class="section-title">Success Stories</h2>
            <div class="row">
                <div class="col-2">
                    <div class="testimonial-card">
                        <div class="testimonial-text">
                            "The Placement Management System made it incredibly easy to connect with top tech companies. I landed my dream job at Google within two weeks of using the platform!"
                        </div>
                        <div class="testimonial-author">
                            <div class="author-avatar">SD</div>
                            <div>
                                <h4>Sarah Davis</h4>
                                <p>Software Engineer at Google</p>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-2">
                    <div class="testimonial-card">
                        <div class="testimonial-text">
                            "As a recruiter, I've found the platform to be extremely efficient. The quality of candidates is exceptional, and the interface makes the hiring process smooth."
                        </div>
                        <div class="testimonial-author">
                            <div class="author-avatar">MJ</div>
                            <div>
                                <h4>Michael Johnson</h4>
                                <p>HR Manager at Microsoft</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Terms & Conditions Section -->
        <section class="section">
            <h2 class="section-title">Terms & Conditions</h2>
            <div class="card">
                <div class="card-body">
                    <h3>User Agreement</h3>
                    <p>By using the Placement Management System, you agree to the following terms:</p>
                    <ul>
                        <li>All information provided must be accurate and truthful</li>
                        <li>Users are responsible for maintaining the confidentiality of their accounts</li>
                        <li>The platform reserves the right to remove inappropriate content</li>
                        <li>Companies must adhere to equal opportunity employment practices</li>
                        <li>Students should respond to interview requests in a timely manner</li>
                        <li>The system administrators may use anonymized data for analytics and improvements</li>
                    </ul>
                    <p>For complete terms and conditions, please refer to our detailed policy document available upon request.</p>
                </div>
            </div>
        </section>
    </div>

    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Placement Management System</h3>
                    <p>Connecting talented students with leading companies for successful career placements.</p>
                </div>
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <ul>
                        <li><a href="/">Home</a></li>
                        <li><a href="#features">Features</a></li>
                        <li><a href="#about">About</a></li>
                        <li><a href="#testimonials">Testimonials</a></li>
                        <li><a href="/contact">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Contact Info</h3>
                    <ul>
                        <li><i class="fas fa-map-marker-alt"></i> Maharishi Markandeshwar Deemed To Be University,MUllana, Ambala,Haryana</li>
                        <li><i class="fas fa-phone"></i>+91 9905812926</li>
                        <li><i class="fas fa-envelope"></i>placementms@gmail.com</li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2023 Placement Management System. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
    <script>
        const mobileNavToggle = document.querySelector('.mobile-nav-toggle');
        const navLinks = document.querySelector('.nav-links');
        const navAnchors = document.querySelectorAll('.nav-links a');

        // Toggle menu on hamburger click
        mobileNavToggle.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        // Close menu when clicking on any nav link
        navAnchors.forEach(anchor => {
            anchor.addEventListener('click', () => {
                navLinks.classList.remove('active');
            });
        });

        // Button ripple animation effect
        function createRipple(event) {
            const button = event.currentTarget;
            const ripple = document.createElement('span');
            
            const rect = button.getBoundingClientRect();
            const size = Math.max(rect.width, rect.height);
            const x = event.clientX - rect.left - size / 2;
            const y = event.clientY - rect.top - size / 2;
            
            ripple.style.width = ripple.style.height = size + 'px';
            ripple.style.left = x + 'px';
            ripple.style.top = y + 'px';
            ripple.classList.add('ripple');
            
            button.appendChild(ripple);
            
            setTimeout(() => ripple.remove(), 600);
        }

        // Add ripple effect to all buttons
        const allButtons = document.querySelectorAll('.btn');
        allButtons.forEach(button => {
            button.addEventListener('click', createRipple);
        });
    </script>
</body>
</html>